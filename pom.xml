<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.zartra.bedwars</groupId>
  <artifactId>zartrabedwars-parent</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>ZartraBedWars Parent</name>
  <description>ZartraBedWars multi-module build and deterministic quality-governance parent.</description>

  <modules>
    <module>build/zbw-bom</module>
    <module>build/zbw-build-tools</module>
    <module>api/zbw-api</module>
    <module>domain/zbw-domain</module>
    <module>application/zbw-application</module>
    <module>configuration/zbw-config</module>
    <module>observability/zbw-observability</module>
    <module>storage/zbw-storage-api</module>
    <module>storage/zbw-storage-sql</module>
    <module>sdk/zbw-sdk</module>
    <module>integrations/discord/zbw-integration-discord-api</module>
    <module>integrations/placeholderapi/zbw-integration-placeholderapi-api</module>
    <module>integrations/placeholderapi/zbw-integration-placeholderapi</module>
    <module>compatibility/zbw-compat-api</module>
    <module>world/zbw-world</module>
    <module>arena/zbw-arena</module>
    <module>game/zbw-game</module>
    <module>command/zbw-command-api</module>
    <module>ui/zbw-ui-api</module>
    <module>scripting/zbw-scripting-api</module>
    <module>scripting/zbw-scripting-engine</module>
    <module>shop/zbw-shop</module>
    <module>content/zbw-content</module>
    <module>progression/zbw-progression</module>
    <module>statistics/zbw-statistics</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <maven.compiler.source>8</maven.compiler.source>
    <maven.compiler.target>8</maven.compiler.target>
    <maven.compiler.parameters>true</maven.compiler.parameters>
    <junit.version>5.11.4</junit.version>
    <maven.compiler.plugin.version>3.14.1</maven.compiler.plugin.version>
    <maven.resources.plugin.version>3.3.1</maven.resources.plugin.version>
    <maven.jar.plugin.version>3.4.1</maven.jar.plugin.version>
    <maven.surefire.plugin.version>3.5.4</maven.surefire.plugin.version>
    <maven.failsafe.plugin.version>3.5.4</maven.failsafe.plugin.version>
    <maven.enforcer.plugin.version>3.6.1</maven.enforcer.plugin.version>
    <maven.shade.plugin.version>3.6.1</maven.shade.plugin.version>
    <jacoco.plugin.version>0.8.13</jacoco.plugin.version>
    <checkstyle.plugin.version>3.6.0</checkstyle.plugin.version>
    <checkstyle.version>10.21.2</checkstyle.version>
    <spotbugs.plugin.version>4.9.2.0</spotbugs.plugin.version>
    <spotbugs.version>4.9.2</spotbugs.version>
    <owasp.dependency-check.version>12.1.0</owasp.dependency-check.version>
    <flyway.version>10.20.1</flyway.version>
    <hikaricp.version>4.0.3</hikaricp.version>
    <caffeine.version>2.9.3</caffeine.version>
    <sqlite-jdbc.version>3.46.0.0</sqlite-jdbc.version>
    <mariadb-driver.version>3.4.1</mariadb-driver.version>
    <mysql-driver.version>8.4.0</mysql-driver.version>
    <testcontainers.version>1.20.4</testcontainers.version>
    <paper-api-mirror.version>1.21.1-build133</paper-api-mirror.version>
  </properties>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven.compiler.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-resources-plugin</artifactId>
          <version>${maven.resources.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>${maven.jar.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven.surefire.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>${maven.failsafe.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-enforcer-plugin</artifactId>
          <version>${maven.enforcer.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-shade-plugin</artifactId>
          <version>${maven.shade.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.jacoco</groupId>
          <artifactId>jacoco-maven-plugin</artifactId>
          <version>${jacoco.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-checkstyle-plugin</artifactId>
          <version>${checkstyle.plugin.version}</version>
          <dependencies>
            <dependency>
              <groupId>com.puppycrawl.tools</groupId>
              <artifactId>checkstyle</artifactId>
              <version>${checkstyle.version}</version>
            </dependency>
          </dependencies>
          <configuration>
            <configLocation>${maven.multiModuleProjectDirectory}/config/checkstyle/checkstyle.xml</configLocation>
            <includeTestSourceDirectory>true</includeTestSourceDirectory>
            <failOnViolation>true</failOnViolation>
          </configuration>
        </plugin>
        <plugin>
          <groupId>com.github.spotbugs</groupId>
          <artifactId>spotbugs-maven-plugin</artifactId>
          <version>${spotbugs.plugin.version}</version>
          <configuration>
            <excludeFilterFile>${maven.multiModuleProjectDirectory}/config/spotbugs/exclude.xml</excludeFilterFile>
            <effort>Max</effort>
            <threshold>High</threshold>
            <failOnError>true</failOnError>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.owasp</groupId>
          <artifactId>dependency-check-maven</artifactId>
          <version>${owasp.dependency-check.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <encoding>UTF-8</encoding>
          <parameters>true</parameters>
          <showWarnings>true</showWarnings>
          <compilerArgs>
            <arg>-Xlint:all,-options</arg>
            <arg>-Werror</arg>
          </compilerArgs>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <useModulePath>false</useModulePath>
          <trimStackTrace>false</trimStackTrace>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <executions>
          <execution>
            <id>enforce-foundation</id>
            <goals><goal>enforce</goal></goals>
            <configuration>
              <rules>
                <requireJavaVersion><version>[1.8,22)</version></requireJavaVersion>
                <requireMavenVersion><version>[3.9.11,3.9.12)</version></requireMavenVersion>
                <requireReleaseDeps><onlyWhenRelease>true</onlyWhenRelease></requireReleaseDeps>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <executions>
          <execution><id>coverage-agent</id><goals><goal>prepare-agent</goal></goals></execution>
          <execution><id>coverage-report</id><phase>verify</phase><goals><goal>report</goal></goals></execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>modern-paper-platform</id>
      <activation><jdk>[21,22)</jdk></activation>
      <modules>
        <module>compatibility/zbw-compat-v1_20-v1_21</module>
        <module>command/zbw-command-paper</module>
        <module>ui/zbw-ui-paper</module>
        <module>platform/paper/zbw-paper-modern</module>
      </modules>
    </profile>
    <profile>
      <id>placeholderapi-runtime</id>
      <modules>
        <module>integrations/placeholderapi/zbw-integration-placeholderapi-runtime</module>
      </modules>
    </profile>
    <profile>
      <id>quality</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-checkstyle-plugin</artifactId>
            <executions>
              <execution><id>checkstyle</id><phase>verify</phase><goals><goal>check</goal></goals></execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>com.github.spotbugs</groupId>
            <artifactId>spotbugs-maven-plugin</artifactId>
            <executions>
              <execution><id>spotbugs</id><phase>verify</phase><goals><goal>check</goal></goals></execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>