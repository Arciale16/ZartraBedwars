/**
 * Explicit capability declarations used by extensions, declarative actions and providers.
 * Capability possession never grants permission by itself; runtime authorization intersects the
 * declared capability, host policy and actor permission.
 */
package io.zartra.bedwars.api.capability;
