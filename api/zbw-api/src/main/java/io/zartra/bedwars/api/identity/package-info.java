/**
 * Immutable, serialization-safe identifiers used by public ZartraBedWars contracts.
 *
 * <p>All parameters and return values are non-null unless their JavaDoc explicitly says
 * otherwise. UUID-backed identifiers are suitable for generated entity identity; namespaced
 * identifiers are suitable for definitions and extension-owned registry entries.</p>
 */
package io.zartra.bedwars.api.identity;
