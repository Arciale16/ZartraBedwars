/**
 * Immutable configuration identities and reload classifications.
 *
 * <p>The package contains no parser, filesystem, platform or runtime-configuration implementation.
 * Implementations live in {@code zbw-config}; keys and versions remain binary compatible within
 * the public API major version.</p>
 */
package io.zartra.bedwars.api.configuration;
