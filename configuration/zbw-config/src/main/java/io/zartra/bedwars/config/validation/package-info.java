/**
 * Startup and operator-invoked configuration validation orchestration.
 *
 * <p>Document loading, platform inspection and cross-document checks are injected ports. The
 * service remains synchronous and platform-free while producing one deterministic actionable
 * report for all 36 logical files and external compatibility facts.</p>
 */
package io.zartra.bedwars.config.validation;
