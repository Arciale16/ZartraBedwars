/**
 * Typed, versioned configuration schemas and deterministic validation.
 *
 * <p>All values are parsed before publication. Unknown, malformed, missing, incompatible or
 * dependency-invalid values are rejected without logging source text. This package performs no
 * filesystem or platform access and is safe to use synchronously during startup or manual checks.</p>
 */
package io.zartra.bedwars.config.schema;
