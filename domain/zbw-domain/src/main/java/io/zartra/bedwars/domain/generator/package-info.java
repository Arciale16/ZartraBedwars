/**
 * Immutable resource and generator definitions.
 *
 * <p>These types model native and custom resources identically. They contain no scheduler or
 * platform behavior; generator execution and configuration loading begin in later milestones.</p>
 */
package io.zartra.bedwars.domain.generator;
