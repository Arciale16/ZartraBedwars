/**
 * Private-game modifier contracts allocated to M02.
 *
 * <p>The original RESOURCE SCARCITY modifier is represented without applying it to a running
 * match. Host authorization, GUI, configuration and generator scheduling remain in M20.</p>
 */
package io.zartra.bedwars.domain.privategame;
