package io.zartra.bedwars.paper.replay.viewer;

/** Stable viewer actions exposed by the command foundation. */
public enum ViewerControlAction {
    VIEW,
    INFO,
    PLAY,
    PAUSE,
    RESUME,
    SPEED,
    STOP,
    SEEK
}
