/**
 * Original, licensed-for-this-project starter shop definitions and balance profiles.
 *
 * <p>Definitions are immutable Java 8 values with stable IDs. Configuration adapters may replace
 * or extend them only after validating a complete catalog and atomically swapping the resulting
 * snapshot. This module does not read files, access a platform or execute item mechanics.</p>
 */
package io.zartra.bedwars.content.shop;
