/**
 * Bounded Java 8 declarative interpreter. Graphs are immutable data, execute only registered
 * allowlisted actions and have no host, reflection, filesystem, process, network or thread API.
 */
package io.zartra.bedwars.scripting.engine;
